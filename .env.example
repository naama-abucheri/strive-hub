# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database

# PostgreSQL Connection Details
PGHOST=localhost
PGPORT=5432
PGDATABASE=strivehub
PGUSER=username
PGPASSWORD=password

# Replit Authentication
SESSION_SECRET=your-session-secret-key-here
REPL_ID=your-repl-id
ISSUER_URL=https://replit.com/oidc
REPLIT_DOMAINS=your-repl-domain.repl.co

# Application Configuration
PORT=5000
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual configuration
# 3. For local development, set up a PostgreSQL database
# 4. For Replit deployment, these values will be automatically configured
# 5. Never commit your actual .env file to version control
